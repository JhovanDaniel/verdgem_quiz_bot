<div class="container mt-4">
  <div class="row">
    <div class="col">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-white">
          <li class="breadcrumb-item"><%= link_to "Subjects", subjects_path %></li>
          <li class="breadcrumb-item"><%= link_to @topic.subject.name, subject_path(@topic.subject) %></li>
          <li class="breadcrumb-item active" aria-current="page"><%= link_to @topic.name, topic_path(@topic) %></li>
          <li class="breadcrumb-item active" aria-current="page">Edit Topic</li>
        </ol>
      </nav>
    </div>
  </div>

  <h2>Edit Topic: <%= @topic.name %></h2>

  <%= render 'topics/form' %>
</div>